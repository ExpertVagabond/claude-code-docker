# Claude Code Docker - Secure Environment Configuration
# ==================================================
#
# SECURITY NOTICE: This file contains sensitive configuration.
# - Never commit this file to version control
# - Use environment-specific values
# - Rotate API keys regularly
# - Limit scope of API access

# Claude API Configuration
# Get your API key from: https://console.anthropic.com/
# Recommended: Use a dedicated API key with limited scope for Docker use
CLAUDE_API_KEY=sk-ant-api03-your-secure-key-here

# Docker Security Settings
# Recommended: Use socket mounting (more secure than full DinD)
DOCKER_MODE=socket  # Options: socket, dind

# Network Security
# Limit exposed ports for production use
EXPOSE_DOCKER_PORT=false  # Set to true only if you need remote Docker access
EXPOSE_WEB_PORTS=true     # Ports 3000, 8080 for development

# Workspace Security
# Define allowed workspace operations
WORKSPACE_READ_ONLY=false  # Set to true for read-only workspace
WORKSPACE_BACKUP=true      # Enable workspace backups

# Environment
ENVIRONMENT=development    # Options: development, testing, production

# Logging and Monitoring
LOG_LEVEL=info            # Options: debug, info, warn, error
ENABLE_AUDIT_LOG=true     # Track container operations

# Security Hardening
ENABLE_APPARMOR=false     # Enable AppArmor security profiles (Linux only)
ENABLE_SECCOMP=true       # Enable secure computing mode
DROP_CAPABILITIES=true    # Drop unnecessary container capabilities